import { stringToArrayBuffer } from './tansform';

async function encrypt(text, keyStr) {
    const api = window.crypto.subtle;
    //const key = api.generateKey()
   // const arrayBuffer = await stringToArrayBuffer(text);
   // api.encrypt('AesCtrParams', 'gland2015', arrayBuffer);

}


function decrypt() {

}

function digest() {

}